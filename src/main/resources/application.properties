# Database Configuration
spring.application.name=jpa
spring.datasource.url=jdbc:mysql://localhost:3306/hospitalDB?useSSL=false
spring.datasource.username=root
spring.datasource.password=alphayash59
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# for initializing data from file to db
spring.jpa.defer-datasource-initialization=true
# means har baar sql run hoga
spring.sql.init.mode=always
spring.sql.init.data-location=classpath:data.sql